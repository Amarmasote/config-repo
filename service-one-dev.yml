service-one:
  message: "Hello from Service One - DEV Environment"

server:
  port: 8082
# Common properties shared across all services & environments
common:
  message: "This property is available to every service in every environment"
logging:
  level:

    root: INFO
